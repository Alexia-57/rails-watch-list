<div class ="container mt-5">
  <h1><%= @list.name %></h1>
  <div>
    <%= link_to "Back", lists_path(@list), class: "btn btn-primary" %>
    <!-- <%= button_to "Remove list", @list, method: :delete, class: "btn btn-danger" %> -->
  </div>
</div>
